### Local environment variables for development
# Copy to `.env` and replace placeholder values with your real keys before running in development.
# Do NOT commit your `.env` file with real secrets to the repository.

PUBLIC_WEB3FORMS_KEY=pk_live_abcdef123456
WEB3FORMS_SECRET=sk_live_abcdef123456

# DEPLOY_ID is optionally used by the Netlify adapter for skew protection and other features
DEPLOY_ID=dev
